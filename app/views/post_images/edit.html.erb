<h1>投稿を編集する</h1>

  <%= form_with model: @post_image do |f| %>
    <h4>画像</h4>
    <%= f.attachment_field :image %>
    
    <h4>投稿内容</h4>
    <%= f.text_area :introduction %>

    <%= f.submit "変更を保存する" %>
  <% end %>